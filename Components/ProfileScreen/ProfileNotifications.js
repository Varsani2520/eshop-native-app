import { View, Text } from "react-native";
import React from "react";

const ProfileNotifications = () => {
  return (
    <View>
      <Text>ProfileNotifications</Text>
    </View>
  );
};

export default ProfileNotifications;
